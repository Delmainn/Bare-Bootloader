#ifndef INC_COMMON_DEFINES_H
#define INC_COMMON_DEFINES_H

#include <stdint.h>      // gives you uint32_t, uint8_t, etc. instead of relying on int/long and hoping the width is right
#include <stdbool.h>    // gives you bool/true/false as real keywords instead of #define hacks

#endif // INC_COMMON_DEFINES_H